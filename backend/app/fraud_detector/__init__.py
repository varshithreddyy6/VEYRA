"""ML pipeline package for the CREDIT CARD FRAUD DETECTION SYSTEM.

Sub-packages
------------
data         — loading, schema/type validation, quality checks
features     — deterministic feature engineering (train and inference share it)
models       — baseline (Logistic Regression) and XGBoost estimators
evaluation   — metrics, threshold optimization, reporting, figures
explainability — SHAP global + local explanations
utils        — seeds, artifact persistence, metadata helpers
"""
